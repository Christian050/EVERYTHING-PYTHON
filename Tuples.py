# 2:13:33

numbers = (1, 2, 3)
print(numbers[0])



'''
Tuples are immutable

Tuples have two methods from list,
.count() to count the occurrences of an item(object)
and .index() to find index of the first occurrence of an item(object)
and magic methods.

'''